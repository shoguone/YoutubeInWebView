namespace YoutubeInWebView.UI.Controls
{
    public enum PlayerState
    {
        NOT_STARTED = -1,
        ENDED = 0,
        PLAYING = 1,
        PAUSED = 2,
        BUFFERING = 3,
        CUED = 5,
    }
}
